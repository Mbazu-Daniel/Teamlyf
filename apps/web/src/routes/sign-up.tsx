import { useState, type FormEvent } from "react";
import { Link, createFileRoute } from "@tanstack/react-router";
import { signUp } from "@/lib/api";
import { Button } from "@/components/ui/button";
import { AuthLayout } from "@/components/auth-layout";
export const Route = createFileRoute("/sign-up")({ component: SignUp });
function SignUp() { const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); setMessage(""); setLoading(true); try { await signUp(String(form.get("name")), String(form.get("email")), String(form.get("password"))); setMessage("Account created. Check your email, then sign in to create your workspace."); } catch (reason) { setMessage(reason instanceof Error ? reason.message : "Unable to create account"); } finally { setLoading(false); } } return <AuthLayout title="Create your workspace" description="Start with a Teamlyf account. You can invite the rest of the team later."><form className="space-y-4" onSubmit={submit}>{[["Name", "name", "Your name", "text"], ["Work email", "email", "you@company.com", "email"], ["Password", "password", "At least 8 characters", "password"]].map(([label, name, placeholder, type]) => <label key={name} className="block text-sm font-semibold">{label}<input className="mt-1.5 h-11 w-full rounded-xl border bg-white px-3 text-sm outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100" name={name} type={type} minLength={name === "password" ? 8 : undefined} placeholder={placeholder} required /></label>)}<p className="text-xs leading-5 text-muted-foreground">By continuing, you agree to use Teamlyf for work you are authorized to manage.</p><Button className="h-11 w-full" type="submit" disabled={loading}>{loading ? "Creating account…" : "Create account"}</Button></form>{message && <p role="status" className="mt-4 rounded-xl bg-violet-50 p-3 text-sm leading-6 text-violet-800">{message}</p>}<p className="mt-6 text-center text-sm text-muted-foreground">Already have an account? <Link className="font-semibold text-violet-700" to="/sign-in">Sign in</Link></p></AuthLayout>; }
