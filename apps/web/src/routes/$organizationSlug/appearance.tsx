import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
export const Route=createFileRoute("/$organizationSlug/appearance")({component:AppearancePage});
function AppearancePage(){const [accent,setAccent]=useState("violet"); const accents=["violet","blue","green","orange","rose"]; return <div className="mx-auto max-w-4xl p-6 sm:p-8"><h1 className="text-2xl font-semibold">Appearance</h1><p className="mt-1 text-sm text-muted-foreground">Customize how Teamlyf looks for you.</p><section className="mt-8 rounded-xl border bg-card p-6"><h2 className="font-semibold">Accent color</h2><p className="mt-1 text-sm text-muted-foreground">Choose the accent used for buttons, links and highlights.</p><div className="mt-5 flex flex-wrap gap-3">{accents.map(color=><button key={color} onClick={()=>setAccent(color)} className={`rounded-lg border px-4 py-2 text-sm capitalize ${accent===color?"ring-2 ring-primary":"hover:bg-muted"}`}>{color}</button>)}</div></section></div>}