import { IconCalendarEvent,IconChevronLeft,IconChevronRight,IconPlus } from "@tabler/icons-react";
import { useMemo,useState } from "react";
export function SchedulePage(){const [month,setMonth]=useState(()=>new Date());const days=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1);const start=new Date(first);start.setDate(1-first.getDay());return Array.from({length:42},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d})},[month]);const label=month.toLocaleDateString(undefined,{month:"long",year:"numeric"});return <div className="flex h-full min-h-0 flex-col gap-5 overflow-y-auto p-3 md:p-4"><section className="rounded-[24px] border bg-white/90 p-6 shadow-sm"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">Month View</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em]">{label}</h1><p className="mt-2 text-sm text-muted-foreground">Plan meetings, tasks and events from one calendar.</p></div><button className="inline-flex h-10 items-center gap-2 rounded-[10px] bg-primary px-4 text-sm font-semibold text-primary-foreground"><IconPlus className="size-4"/>New event</button></div></section><section className="overflow-hidden rounded-[24px] border bg-card shadow-sm"><div className="flex items-center justify-between border-b bg-muted/20 p-4"><div className="flex items-center gap-1 rounded-[10px] bg-muted p-1"><button onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))} className="grid size-9 place-items-center rounded-lg hover:bg-background"><IconChevronLeft className="size-4"/></button><button onClick={()=>setMonth(new Date())} className="h-9 rounded-lg px-3 text-sm font-medium hover:bg-background">Today</button><button onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))} className="grid size-9 place-items-center rounded-lg hover:bg-background"><IconChevronRight className="size-4"/></button></div><span className="hidden items-center gap-2 text-xs text-muted-foreground sm:flex"><IconCalendarEvent className="size-4"/>Calendar</span></div><div className="grid grid-cols-7 border-b bg-muted/30">{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(d=><div key={d} className="py-3 text-center text-[10px] font-bold uppercase tracking-[.18em] text-muted-foreground">{d}</div>)}</div><div className="grid grid-cols-7">{days.map(day=>{const current=day.getMonth()===month.getMonth();const today=new Date();const isToday=day.toDateString()===today.toDateString();return <button key={day.toISOString()} className={"min-h-[120px] border-b border-r p-2 text-left transition hover:bg-muted/30 "+(current?"bg-background":"bg-muted/20")}><span className={"flex size-7 items-center justify-center rounded-full text-xs font-semibold "+(isToday?"bg-primary text-primary-foreground":current?"":"text-muted-foreground")}>{day.getDate()}</span></button>})}</div></section></div>}