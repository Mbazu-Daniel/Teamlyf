import { IconChevronLeft, IconChevronRight, IconPlus } from "@tabler/icons-react";
import { useMemo, useState } from "react";

export function SchedulePage() {
  const [offset, setOffset] = useState(0);
  const days = useMemo(() => Array.from({ length: 7 }, (_, index) => { const date = new Date(); date.setDate(date.getDate() + offset * 7 + index); return date; }), [offset]);
  return <div className="flex h-full min-h-0 flex-col bg-[var(--app-page-background)]"><header className="flex items-center justify-between border-b bg-card px-5 py-4"><div><h1 className="text-xl font-semibold">Schedule</h1><p className="text-xs text-muted-foreground">Plan your week and keep work visible.</p></div><button className="inline-flex items-center gap-2 rounded-lg bg-primary px-3 py-2 text-sm font-medium text-primary-foreground"><IconPlus className="size-4"/> New event</button></header><div className="flex items-center justify-between border-b bg-card px-5 py-3"><button onClick={() => setOffset(offset - 1)} className="grid size-8 place-items-center rounded-lg hover:bg-muted"><IconChevronLeft className="size-4"/></button><p className="text-sm font-medium">{days[0].toLocaleDateString(undefined,{month:"long",day:"numeric"})} - {days[6].toLocaleDateString(undefined,{month:"long",day:"numeric",year:"numeric"})}</p><button onClick={() => setOffset(offset + 1)} className="grid size-8 place-items-center rounded-lg hover:bg-muted"><IconChevronRight className="size-4"/></button></div><div className="min-h-0 flex-1 overflow-auto"><div className="grid min-w-[840px] grid-cols-7 divide-x">{days.map((day) => <div key={day.toISOString()} className="min-h-[620px]"><div className="sticky top-0 border-b bg-card p-3 text-center"><p className="text-[11px] uppercase text-muted-foreground">{day.toLocaleDateString(undefined,{weekday:"short"})}</p><p className="mt-1 text-lg font-semibold">{day.getDate()}</p></div><div className="h-full bg-background/40 p-2"><div className="h-16 border-b border-dashed"/><div className="h-16 border-b border-dashed"/><div className="h-16 border-b border-dashed"/><div className="h-16 border-b border-dashed"/><div className="h-16 border-b border-dashed"/></div></div>)}</div></div></div>;
}