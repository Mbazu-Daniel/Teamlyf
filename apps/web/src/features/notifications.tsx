import { IconBell,IconCircleCheck,IconExternalLink } from "@tabler/icons-react"; import { useState } from "react";
type Notification={id:string;title:string;body:string;time:string;read:boolean};
const initial:Notification[]=[{id:"1",title:"Notifications are ready",body:"Your organization activity will appear here.",time:"Just now",read:false},{id:"2",title:"Stay on top of your work",body:"Task, project and team updates will appear in this inbox.",time:"Today",read:true}];
export function NotificationsPage(){const [items,setItems]=useState(initial);const unread=items.filter(x=>!x.read).length;return <div className="mx-auto flex h-full w-full max-w-[960px] flex-col gap-5 overflow-y-auto p-4 md:p-8"><section className="rounded-[16px] border bg-white/90 p-6 shadow-sm"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2"><IconBell className="size-5 text-primary"/><h1 className="text-2xl font-semibold">Notifications</h1>{unread>0?<span className="rounded-full bg-primary px-2 py-0.5 text-[10px] font-bold text-primary-foreground">{unread}</span>:null}</div><p className="mt-1 text-sm text-muted-foreground">Updates and activity from your organization.</p></div><button onClick={()=>setItems(items.map(x=>({...x,read:true})))} className="inline-flex h-10 items-center gap-2 rounded-[10px] border px-3 text-sm font-medium hover:bg-muted"><IconCheckCircle className="size-4"/>Mark all as read</button></div></section><section className="overflow-hidden rounded-[16px] border bg-card shadow-sm">{items.map(item=><button key={item.id} onClick={()=>setItems(items.map(x=>x.id===item.id?{...x,read:true}:x))} className={"group flex w-full gap-4 border-b p-5 text-left last:border-b-0 hover:bg-muted/40 "+(!item.read?"bg-primary/[0.035]":"")}><span className={"mt-1 grid size-10 shrink-0 place-items-center rounded-full "+(item.read?"bg-muted text-muted-foreground":"bg-primary/10 text-primary")}><IconBell className="size-4"/></span><span className="min-w-0 flex-1"><span className="flex items-start justify-between gap-4"><span className={"text-sm "+(item.read?"font-medium":"font-semibold")}>{item.title}</span><span className="shrink-0 text-[11px] text-muted-foreground">{item.time}</span></span><span className="mt-1 block text-sm leading-6 text-muted-foreground">{item.body}</span></span><IconExternalLink className="mt-1 size-4 shrink-0 text-muted-foreground opacity-0 transition group-hover:opacity-100"/></button>)}</section></div>;}