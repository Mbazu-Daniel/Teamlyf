import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";
import { useMemo,useState } from "react";
import { IconBriefcase,IconCalendar,IconFileText,IconLock,IconPlus,IconSearch,IconStar,IconTrash,IconUser } from "@tabler/icons-react";
import { notesApi } from "@/lib/api";

const categories=[["favorites","Favorites",IconStar],["private","Private",IconLock],["personal","Personal",IconUser],["work","Work",IconBriefcase],["events","Events",IconCalendar]] as const;
export function NotesPage({organizationId}:{organizationId:string}){
 const qc=useQueryClient(); const [selectedId,setSelectedId]=useState<string>(); const [search,setSearch]=useState(""); const [category,setCategory]=useState<string|null>(null); const [title,setTitle]=useState(""); const [content,setContent]=useState("");
 const notes=useQuery({queryKey:["notes",organizationId],queryFn:()=>notesApi.list(organizationId)});
 const filtered=useMemo(()=> (notes.data??[]).filter(n=>{const match=n.title.toLowerCase().includes(search.toLowerCase()); if(!match)return false; if(category==="favorites")return Boolean((n as {isFavorite?:boolean}).isFavorite); if(category==="private")return (n as {isPublic?:boolean}).isPublic===false; return true}),[notes.data,search,category]);
 const selected=filtered.find(n=>n.id===selectedId)??filtered[0];
 const create=useMutation({mutationFn:()=>notesApi.create(organizationId,{title:title.trim()||"Untitled note",content}),onSuccess:n=>{setTitle("");setContent("");setSelectedId(n.id);void qc.invalidateQueries({queryKey:["notes",organizationId]})}});
 const update=useMutation({mutationFn:()=>selected?notesApi.update(organizationId,selected.id,{title:title||selected.title,content:content||selected.content}):Promise.resolve(null),onSuccess:()=>void qc.invalidateQueries({queryKey:["notes",organizationId]})});
 const remove=useMutation({mutationFn:()=>selected?notesApi.remove(organizationId,selected.id):Promise.resolve(),onSuccess:()=>{setSelectedId(undefined);void qc.invalidateQueries({queryKey:["notes",organizationId]})}});
 if(notes.isPending)return <div className="p-8 text-sm text-muted-foreground">Loading notes...</div>;
 return <div className="flex h-full min-h-0 flex-col gap-5 overflow-y-auto p-3 md:p-4"><section className="rounded-[16px] border border-[var(--app-panel-border)] bg-white/90 p-4 shadow-sm"><div className="flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div><h1 className="text-2xl font-bold tracking-tight">Notes</h1><p className="mt-1 text-sm text-muted-foreground">{notes.data?.length??0} {(notes.data?.length??0)===1?"note":"notes"} total</p></div><div className="flex w-full max-w-[720px] gap-3"><div className="relative min-w-0 flex-1"><IconSearch className="absolute left-4 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search notes..." className="h-12 w-full rounded-[14px] border bg-muted/30 pl-11 pr-3 text-sm outline-none focus:ring-2 focus:ring-primary/15"/></div><button onClick={()=>create.mutate()} className="inline-flex h-12 items-center gap-2 rounded-[12px] bg-primary px-4 text-sm font-semibold text-primary-foreground"><IconPlus className="size-4"/>New Note</button></div></div></section><section className="rounded-[16px] border border-[var(--app-panel-border)] bg-white/90 p-4 shadow-sm"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{categories.map(([id,label,Icon])=><button key={id} onClick={()=>setCategory(category===id?null:id)} className={"flex min-h-[112px] flex-col items-start justify-between rounded-[14px] border p-4 text-left transition "+(category===id?"border-primary bg-primary/5 ring-2 ring-primary/10":"bg-muted/20 hover:bg-background")}><span className="grid size-10 place-items-center rounded-[12px] bg-primary/10 text-primary"><Icon className="size-5"/></span><span><span className="block text-sm font-semibold">{label}</span><span className="text-xs text-muted-foreground">{filtered.length} notes</span></span></button>)}</div></section><div className="grid min-h-0 gap-5 xl:grid-cols-[280px_minmax(0,1fr)]"><aside className="rounded-[16px] border border-[var(--app-panel-border)] bg-card p-2"><div className="px-3 py-2 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Recently opened</div><div className="space-y-1">{filtered.map(n=><button key={n.id} onClick={()=>setSelectedId(n.id)} className={"w-full rounded-xl px-3 py-3 text-left "+(selected?.id===n.id?"bg-primary/10 text-primary":"hover:bg-muted")}><p className="truncate text-sm font-medium">{n.title||"Untitled"}</p><p className="mt-1 truncate text-xs text-muted-foreground">{n.content||"Empty note"}</p></button>)}</div></aside><main className="min-w-0 rounded-[16px] border border-[var(--app-panel-border)] bg-white/90 p-5 shadow-sm sm:p-8">{selected?<><div className="flex items-center justify-between"><span className="text-xs text-muted-foreground">Last edited {selected.updatedAt?new Date(selected.updatedAt).toLocaleString():"just now"}</span><button onClick={()=>remove.mutate()} className="grid size-9 place-items-center rounded-lg text-muted-foreground hover:bg-destructive/10 hover:text-destructive"><IconTrash className="size-4"/></button></div><input value={title||selected.title} onChange={e=>setTitle(e.target.value)} onBlur={()=>update.mutate()} className="mt-6 w-full border-0 bg-transparent text-3xl font-semibold outline-none" /><textarea value={content||selected.content} onChange={e=>setContent(e.target.value)} onBlur={()=>update.mutate()} placeholder="Start writing..." className="mt-6 min-h-[55vh] w-full resize-none border-0 bg-transparent text-base leading-7 outline-none"/></>:<div className="flex min-h-[520px] flex-col items-center justify-center text-center"><IconFileText className="size-10 text-muted-foreground/40"/><h2 className="mt-4 text-2xl font-semibold">No notes yet</h2><p className="mt-1 text-sm text-muted-foreground">Create a note to get started.</p></div>}</main></div></div>;
}